class tokens_farm_locators:
    token_chain_dropdown: str = '#farm-chain'
    expanded_dropdown: str = token_chain_dropdown + ' input'
    wallet_connect_close_modal: str = "[class*='web3-wallet-connector'] [class*='close-modal-btn']"
    wallet_connect_buttons: str = "[class*='connect-wallet-buttons'] > div"

